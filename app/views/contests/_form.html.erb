<%= form_for(@contest) do |f| %>
  <% if @contest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contest.errors.count, "error") %> prohibited this contest from being saved:</h2>

      <ul>
      <% @contest.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :contest_start %><br />
    <%= f.datetime_select :contest_start %>
  </div>
  <div class="field">
    <%= f.label :contest_end %><br />
    <%= f.datetime_select :contest_end %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
